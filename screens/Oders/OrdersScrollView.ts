import { Dimensions } from 'react-native';
import { ScrollView } from 'react-native-gesture-handler';
import styled from 'styled-components/native';
import { vars } from '../../values';

export const OrdersScrollView = styled(ScrollView)`
    margin-top: 62px;
    margin-bottom: 20px;
    padding: 15px 0;
`;
