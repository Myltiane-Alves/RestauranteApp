import styled from 'styled-components/native';
import { vars } from '../../values';

export const SchedulesDescription = styled.Text`
font-size: 24px;
text-align: center;
color: ${vars.blue};
font-weight: 700;
`;
