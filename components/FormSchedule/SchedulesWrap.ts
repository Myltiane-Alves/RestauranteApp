import styled from 'styled-components/native';
import { vars } from '../../values';

export const SchedulesWrap = styled.View`
    /* background-color: ${vars.blue}; */
    /* background-color: transparent; */
    padding: ${vars.space}px;
    padding-top: 100px;
    padding-bottom: 100px;
  
`;
