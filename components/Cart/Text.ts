import styled from 'styled-components/native';
import { vars } from '../../values';

export const Text = styled.Text`
    font-size: 22px;
    color: ${vars.white};
    font-weight: bold;
    margin-top: 20px;
    margin-bottom: 40px;
`;
